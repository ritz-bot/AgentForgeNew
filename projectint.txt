### Project Overview: Building a Multi-AI Agent System

This project focuses on creating a customizable multi-AI agent application. Users can select the type of agent (e.g., financial, medical, study-related, or any other) and ask specific questions. For example:
- A medical agent can answer queries about diseases.
- A financial agent can provide info on stocks.
- The agent uses AI to process and respond intelligently.

The demo video (already shown) illustrates how it works: User inputs agent type and question, and the system generates a response. The core idea is to combine an LLM (Large Language Model) with internet search for up-to-date answers, avoiding outdated LLM training data (e.g., cut off at 2021).

### Key Tech Stacks and Their Roles

The project uses a combination of tools for AI, backend, frontend, code quality, CI/CD, and deployment. Here's a breakdown:

1. **Grok (as LLM via Grok Cloud)**:
   - Free to use.
   - Acts as the core language model to generate responses.
   - Offers various models (e.g., Llama, Gemma); in this project, we'll use Llama 3.
   - Limitation: Trained on data up to a certain cutoff (e.g., 2021), so it can't provide latest info (e.g., 2025 stock trends) without external help.

2. **Tavily (Online Search Tool)**:
   - Free API for fetching real-time internet data.
   - Integrated to overcome LLM limitations—e.g., for latest news, stocks, or events.
   - Workflow: User asks a question (e.g., "Top stocks in 2025?") → Tavily searches online → LLM (Grok) analyzes results → Generates accurate, current response.
   - Example: Without Tavily, LLM might suggest 2021 stocks; with it, you get 2025 data.

3. **LangChain (Generative AI Framework)**:
   - Used to interact with the LLM (Grok).
   - Handles prompt engineering, chaining responses, and integrating tools like Tavily.

4. **LangGraph (Agentic AI Framework)**:
   - Built on top of LangChain.
   - Specifically for creating AI agents and their workflows (e.g., decision-making, multi-step processes).
   - Enables "agentic" behavior: Agents can reason, use tools (like search), and execute tasks.

5. **FastAPI (Backend)**:
   - Creates the backend server to handle user requests (e.g., process agent type, question, and forward to AI logic).
   - Receives input from frontend, processes via AI, and returns output.

6. **Streamlit (Frontend/UI)**:
   - Builds a simple web-based UI.
   - User form: Select agent type, enter question, click "Generate".
   - Displays the AI-generated response.
   - Connects to backend for input/output flow.

7. **Docker (Containerization)**:
   - Key for production-grade projects.
   - Packages the entire app (code, dependencies) into containers for easy deployment on any platform (e.g., AWS, GCP).
   - Used in every project in this course.

8. **SonarQube (Code Quality Tool)**:
   - Open-source and free; MVP (Most Valuable Player) of the project along with Jenkins.
   - Scans code for issues to ensure high quality.
   - Focuses on three main areas:
     - **Bugs**: Errors that cause weird behavior. Example: "Dead code bug" – Code that never executes, like a redundant `elif` block in an if-else chain (e.g., two identical conditions for "Hi" – the second one is unreachable).
     - **Code Smells (Bad Practices)**: Not errors, but poor habits affecting readability/maintainability. Examples:
       - A single function with 300-800 lines → Split into smaller functions (e.g., 100 lines each) for better readability and analysis.
       - Using double quotes for very long strings → Use triple quotes or multi-line comments for clarity.
       - Goal: Make code easier to read, debug, and maintain.
     - **Code Duplication**: Repeating the same code in multiple files (e.g., identical functions in app.py, main.py, test.py) → Refactor by creating one function and importing it elsewhere.
   - Also checks security, but focus here is code quality.
   - Integrated into CI/CD to scan before deployment.

9. **Jenkins (CI/CD Pipeline Tool)**:
   - Another MVP; automates the build, test, deploy process.
   - Workflow:
     - GitHub acts as SCM (Source Code Management) to store and track code versions.
     - Jenkins fetches code from GitHub.
     - Builds Docker image using Dockerfile.
     - Pushes image to AWS ECR (Elastic Container Registry – AWS's Docker Hub equivalent).
     - Deploys to AWS ECS Fargate (serverless container service for running apps).
   - Entire process runs with one click (e.g., button in Jenkins).
   - Stages (detailed below in pipeline section).
   - GitHub integration for code versioning.

10. **AWS Services (Deployment)**:
    - **ECR**: Stores Docker images.
    - **ECS Fargate**: Runs the containerized app (serverless, no need to manage servers).
    - Challenge: Integrate a Load Balancer (e.g., ALB – Application Load Balancer) for a static URL. Without it, Fargate gives dynamic URLs that change per deployment. With it, URL stays the same regardless of updates.

### Detailed Explanations of Key Concepts

- **Why Integrate Tavily with LLM?**
  - LLMs like Grok are trained on historical data (e.g., up to 2021). Asking for current info (e.g., 2025 stocks) gives outdated answers.
  - Tavily fetches real-time web data → LLM analyzes → Accurate response.
  - Example: Financial agent query "Top stocks now?" → Without Tavily: 2021 suggestions. With: 2025 data.

- **SonarQube in Depth**:
  - Scans entire codebase.
  - Bugs: Detects issues like dead code (e.g., in if-elif-else: If first condition catches "Hi", a later identical one is "dead" and never runs).
  - Code Smells: Focus on readability. Long functions make debugging hard; split them. Long single-line strings → Use multi-line for clarity.
  - Duplication: Avoid copy-pasting code; use imports/modules.
  - Output: Dashboard shows issues; fix before proceeding.

- **Jenkins CI/CD Pipeline Workflow**:
  - **Step 1: Jenkins Setup** – Install/configure Jenkins on your PC; access dashboard.
  - **Step 2: GitHub Integration** – Create pipeline; connect to GitHub repo; copy code to Jenkins workspace.
  - **Step 3: SonarQube Integration** – Scan code in workspace for quality (bugs, smells, duplication).
  - **Step 4: Build Docker Image** – Use Dockerfile to build image.
  - **Step 5: Push to ECR** – Upload image to AWS ECR.
  - **Step 6: Deploy to ECS Fargate** – Run app from image; access via URL (dynamic by default).
  - **Challenge**: Add Load Balancer for static URL (hint provided; optional but recommended).

### Project Setup and Video Sequence (Step-by-Step Workflow)

The project is divided into videos/steps for building incrementally:

1. **Project Setup Video**:
   - Create environment (e.g., virtual env).
   - Add logging and exception handling (log processes, custom error management).
   - Define project structure: Directories, folders, .env file (for secrets), requirements.txt (dependencies), setup.py.

2. **API Setup**:
   - Grok Cloud: Sign up, get API key, store in .env.
   - Tavily: Sign up (free), get access key, store in .env.

3. **Configuration Code**:
   - Access API keys from .env.
   - Choose LLM model (e.g., Llama 3.1 Instant or Versatile).

4. **Core Code Video (Heart of the App)**:
   - Write main logic: Integrate LangChain (for LLM interaction), LangGraph (for agents/workflows), Tavily (search).
   - Handle user input: Agent type + question → Process → Response.

5. **Backend Setup**:
   - Use FastAPI to build server; handle requests/responses.

6. **Frontend Setup**:
   - Use Streamlit for UI; form for inputs, display outputs.

7. **Main Application Video**:
   - Connect backend (FastAPI) with frontend (Streamlit).
   - Test end-to-end: User input → Backend processing → Output display.

8. **Code Versioning**:
   - Push all code to GitHub repo (track changes).

9. **Dockerfile Creation**:
   - Write Dockerfile to containerize the app.

10. **Jenkins Pipeline**:
    - As detailed above: Setup, integrate GitHub/SonarQube, build/push/deploy.

11. **Deployment and Challenge**:
    - App runs on AWS Fargate.
    - Optional: Add Load Balancer for static URL.

### Final Notes
This setup creates a production-grade AI agent app: Scalable, maintainable, and deployable. Focus on code quality (SonarQube) and automation (Jenkins) to avoid common pitfalls. If you follow this structure during revision, the flow should be crystal clear—start from setup, build core AI, add front/back ends, then automate deployment. If doubts arise, refer back to specific sections like SonarQube examples or pipeline stages for clarity.

8.91


### Project Setup Video Summary

This is a detailed step-by-step summary of the video transcript for setting up the project structure, API structure, and URL structure for a "Multi AI Agent" project. I've organized it into sections for easy revision, ensuring no details are skipped. The project uses Python, VS Code, virtual environments, and various libraries. References to GitHub are for copying code/files as mentioned.

#### 1. Creating the Project Folder and Opening in VS Code
- Open any folder on your local PC.
- Create a new folder named "multi AI agent" (based on the project theme).
- Double-click to open the folder.
- Right-click inside the folder and select "Open in Terminal."
- In the terminal, type `code .` (full stop) and press Enter. This opens the folder in VS Code.

#### 2. Creating and Activating the Virtual Environment
- In VS Code, go to the top bar, click "Terminal" > "New Terminal," and select "Command Prompt" (ensure it's Command Prompt, not PowerShell).
- In the Command Prompt terminal, type: `python -m venv env` and press Enter.
  - This creates a virtual environment named "env" (visible on the left side after 10-15 seconds).
- Activate the virtual environment: Type `env\Scripts\activate` and press Enter.
  - You'll see "(env)" in the terminal prompt, confirming activation.

#### 3. Creating requirements.txt File
- Close the terminal if needed.
- In VS Code, create a new file named `requirements.txt`.
- List the following libraries inside it (one per line):
  - `langchain_groq` (for Groq API integration).
  - `langchain_community` (community library for LangChain).
  - `python-dotenv` (for handling environment variables; use case explained later).
  - `uvicorn` (for API setup in the backend).
  - `fastapi` (for creating the API structure).
  - `pydantic` (for data validation and settings).
  - `streamlit` (for creating the UI/frontend).
  - `langgraph` (for creating AI agents in a multi-agent project).
- Note: You can copy this file from the GitHub link provided in the code section instead of writing it manually.

#### 4. Creating setup.py File
- Create a new file named `setup.py`.
- Copy the code for `setup.py` from the GitHub code section (a separate video explains this file in detail).
- Modify the project name in the code (e.g., to "multi AI agent project" or as desired).
- This file handles project dependencies and installation.

#### 5. Installing Dependencies
- Open the terminal (use the previous Command Prompt one).
- Type `pip install -e .` (hyphen e full stop) and press Enter.
  - This triggers `setup.py`, which installs all libraries from `requirements.txt`.
  - Wait for installation to complete (it installs project dependencies).

#### 6. Project Structure Setup
- Inside the main project folder ("multi AI agent"), create a subfolder named `app`. All project components (backend, UI, etc.) will be inside this.
- Inside `app`, create the following subfolders:
  - `backend`: For API setup and backend logic.
  - `frontend`: For UI (Streamlit code).
  - `common`: For logging files and custom exception files (watch separate videos for explanations).
  - `core`: For the main logic of the multi-agent system.
  - `config`: For API setup, loading APIs, etc.
- Inside `app`, create a file named `main.py`: This is the main app code that connects frontend, backend, etc. Running this starts the app.
- Inside `app`, create an `__init__.py` file (double hyphen init double hyphen dot py).
  - This makes the app folder a package, allowing imports between directories (importance explained in setup.py video).
- Re-run `pip install -e .` after creating `__init__.py` to recognize the project as a package.

#### 7. Setting Up the `common` Folder
- Inside `common`, create an `__init__.py` file (to treat `common` as a package).
- Create two files inside `common`:
  - `logger.py`: For logging (copy code from GitHub; explained in previous modules).
  - `custom_exception.py`: For custom exceptions (copy code from GitHub; explained in previous modules).
- Re-run `pip install -e .` after creating these `__init__.py` and files.
  - This enables importing (e.g., `get_logger` function from `logger.py`) across directories like frontend or backend.
  - If not run, imports won't work between directories.

#### 8. API Setup (.env File and API Keys)
- In the main project folder (outside `app`), create a file named `.env` (for environment variables like API keys).
- Inside `.env`, add:
  - `GROQ_API_KEY=your_key_here`
  - `TAVILY_API_KEY=your_key_here`
- These APIs are used for:
  - Groq API: For Language Models (LMs).
  - Tavily API: For online search.
  - (Explained in project introduction video.)
- To get Groq API key:
  - Open browser, search for "Groq API."
  - Create/log in to account (free).
  - Go to API keys > Create new key (name it e.g., "LM ops").
  - Copy the key and paste into `.env`.
- To get Tavily API key:
  - Search for "Tavily" > Log in (or sign up).
  - In the overview section, copy the default API key.
  - Paste into `.env`.

#### 9. WSL (Windows Subsystem for Linux) Setup
- WSL is used to simulate a Linux environment for production-grade projects (not Windows-native).
- It's needed with Docker Engine for creating Docker apps and Jenkins pipelines later.
- If not installed:
  - Open PowerShell in Administrator mode (every Windows has it).
  - Run: `wsl --install` (copy from GitHub documentation.md file).
- If already installed:
  - Run: `wsl --update` and reboot/restart PC for safety.
- Install Ubuntu:
  - Open Microsoft Store > Search "Ubuntu."
  - Install "Ubuntu 22.04" (or the first/normal Ubuntu; 22.04 is recommended).
  - Open it after install and follow guided steps.

#### 10. Docker Setup on Ubuntu via WSL
- Docker Engine (not Docker Desktop) is installed on Ubuntu for this project.
- Search "Install Docker Engine on Ubuntu" or use commands from GitHub documentation.md.
- Open Ubuntu terminal (after WSL setup).
- Run commands one by one (from documentation):
  - Set up Docker apt repository.
  - Add Docker's official GPG key.
  - Update apt index.
  - Install Docker packages (e.g., `docker-ce`, `docker-ce-cli`, etc.).
- Restart Ubuntu terminal after installation.
- Verify:
  - Type `docker --version` (should show Docker version, e.g., Client: Docker Engine - Community 28.1, Server: Docker Engine).
  - If it shows client and server installed, success. If not, redo steps.
- Note: Docker Engine includes client and server; used similarly to Docker Desktop in other projects.

#### Final Notes from Video
- This completes basic project setup: folder structure, virtual env, logging/custom exceptions, API setup, WSL, and Docker.
- Upcoming videos: Backend setup, frontend setup, core/config/common code.
- Every time you add `__init__.py` files or new directories, re-run `pip install -e .` to update package recognition.
- All code/files (e.g., setup.py, logger.py) available on GitHub for copy-paste.
- Watch referenced videos for deeper explanations (e.g., setup.py, logger.py, custom exceptions, project intro).


8.92


### Configuration Code Video Summary

This is a detailed step-by-step summary of the video transcript for writing the configuration code in the "config" directory for the Multi AI Agent project. I've organized it into sections for easy revision, ensuring no details are skipped. The focus is on setting up API keys, loading them from the .env file, and defining allowed models using Python in settings.py.

#### 1. Navigating to the Config Directory
- Open your project in VS Code and navigate to the `app` directory.
- Inside `app`, go to the `config` directory (created in previous setup).

#### 2. Making Config a Package
- To treat the `config` directory as a Python package (allowing imports), create a file named `__init__.py` (double underscore init double underscore dot py) inside `config`.
  - This file can be empty; its presence makes the directory importable.

#### 3. Creating settings.py
- Inside `config`, create a new file named `settings.py`.
- This file contains the main configuration code for loading APIs and defining models.

#### 4. Imports in settings.py
- At the top of `settings.py`, add the following imports:
  - `from dotenv import load_dotenv` (from the python-dotenv library).
  - `import os` (the built-in OS library).
- Explanation:
  - `dotenv` library: Used to load API keys (Groq API and Tavily API) from the `.env` file you created earlier.
  - `os` library: Used to interact with the operating system, specifically to retrieve environment variables.

#### 5. Loading the .env File
- After the imports, add: `load_dotenv()`
- What it does:
  - Loads all API keys (Groq API and Tavily API) from the `.env` file into the environment, making them accessible in `settings.py`.

#### 6. Creating the Settings Class
- Define a class named `Settings` (capital S).
- Inside the class:
  - Define a variable: `groq_api_key = os.getenv('GROQ_API_KEY')`
    - This loads the Groq API key from the environment (from .env) into this class variable.
    - Important: The string `'GROQ_API_KEY'` must exactly match the key name in `.env` (no spelling mistakes).
  - Similarly: `tavily_api_key = os.getenv('TAVILY_API_KEY')`
    - Loads the Tavily API key the same way.
    - Tip: Copy-paste the key names from `.env` to avoid errors.
- Purpose: Transfers the API keys from the environment (.env) into class variables for easy access.

#### 7. Defining Allowed Models
- Groq API provides access to many models; you need to specify only certain ones for the project.
- Google "Groq API supported models" to see the list of production models.
- Inside the `Settings` class, create a list variable: `allowed_model_names = []`
  - Populate the list with specific model names (copy exactly to avoid issues).
  - Examples from transcript:
    - First model: `'llama-3.1-70b-versatile'` (Llama 3 70 billion parameters, 8192 context; more accurate but slower).
    - Second model: `'llama-3.1-405b-reasoning'` (Llama 3 versatile version; transcript mentions "llama only but versatile version").
  - Other options mentioned:
    - You can use smaller models like 8 billion parameters (e.g., `'llama-3.1-8b-instant'`) for faster responses, though less accurate than 70B.
    - Distil models or others from the Groq list.
- Purpose:
  - Restrict usage to these models only.
  - In the app, give users an option to choose a model (e.g., via a query prompt: select Llama 3 70B or the versatile version).
  - You can expand the list later, but start with 2-3 for now.

#### 8. Instantiating the Settings Class
- After defining the class, add: `settings = Settings()`
- This creates an instance of the class, loading all the variables (API keys and model list).

#### 9. Usage of settings.py
- To access the configurations in other parts of the project:
  - Import from this file: e.g., `from config.settings import settings`
  - Then use: `settings.groq_api_key`, `settings.tavily_api_key`, or `settings.allowed_model_names`.
- Simple and centralized: Whenever you need APIs or models, import and use from here.

#### 10. Final Notes from Video
- This completes the `settings.py` code, which is the main configuration for the project.
- Move to the next video after this.

This setup ensures secure loading of APIs (without hardcoding) and model restrictions. If revising, test by printing the variables (e.g., add `print(settings.groq_api_key)` temporarily) to verify loading. Note: The transcript uses "drop API key" once (likely a typo for "Groq"), "devel API" (likely "Tavily"), and "table api key" (Tavily); I've corrected to match context.

8.93

### Core Code Setup Video Summary

This is a detailed step-by-step summary of the video transcript for setting up the core logic in the `core` directory for the Multi AI Agent project. I've organized it into sections for easy revision, ensuring no details are skipped. The main file created is `ai_agent.py`, which contains the function for getting responses from AI agents using Groq models, optional Tavily search, and LangGraph for agent creation.

#### 1. Navigating to the Core Directory and Making It a Package
- Open your project in VS Code and navigate to the `app` directory.
- Inside `app`, go to the `core` directory (created in previous setup).
- To treat the `core` directory as a Python package (allowing imports), create a file named `__init__.py` (double underscore init double underscore dot py) inside `core`.
- Important: Every time you create an `__init__.py` file, run `pip install -e .` (hyphen e full stop) in the terminal.
  - Open any Command Prompt terminal in the project root.
  - Type: `pip install -e .` and press Enter.
  - This initializes/re-installs all dependencies to recognize the new package.
  - Wait for it to finish installing before proceeding (close the terminal after).

#### 2. Creating ai_agent.py
- Inside `core`, create a new file named `ai_agent.py`.
- This file contains all the core logic of the app, specifically for creating and running AI agents.

#### 3. Imports in ai_agent.py
- First import: `from langchain_groq import ChatGroq`
  - Purpose: This is a class/module for using Groq models (e.g., Llama versatile or 70B models imported in previous config video).
  - Allows access to Language Models (LMs) via Groq API.
- Second import: `from langchain_community.tools.tavily_search import TavilySearchResults`
  - Purpose: For using the Tavily API for online search.
  - Both Groq and Tavily APIs/tools are now imported.
- Third import: `from langgraph.prebuilt import create_react_agent`
  - Purpose: LangGraph is an advanced library from LangChain for creating AI agents (for multi-agent projects).
- Fourth import: `from langchain_core.messages import AIMessage`
  - Purpose: To distinguish AI-generated messages from human ones in conversation history.
  - If `langchain_core` is not installed automatically:
    - Add `langchain_core` to `requirements.txt`.
    - Run `pip install -e .` again.
- Fifth import: `from app.config.settings import settings`
  - Purpose: Imports the `settings` instance from `config/settings.py` (contains Groq API key, Tavily API key, and allowed model names).

#### 4. Creating the Function: get_response_from_ai_agent
- Define a function named `get_response_from_ai_agent`.
- Parameters (what the function requires):
  - `lm_id`: The model name/ID (e.g., Llama 70B versatile, Llama 70B, Mistral models; user chooses which Groq model to use).
  - `query`: The user's question/query.
  - `allow_search`: Boolean (True/False); whether to allow online search (Tavily will be used if True; user gets a checkbox in the app to enable/disable).
  - `system_prompt`: The system prompt to control AI behavior (instructions like "do this, then this"; defined later).
- Purpose: This function sets up and runs an AI agent to respond to the query, optionally using search.

#### 5. Setting Up the Language Model (LM) Inside the Function
- `lm = ChatGroq(model=lm_id)`
  - Uses the imported ChatGroq class.
  - Sets the model to whatever `lm_id` the user passes (e.g., 'llama-3.1-70b-versatile' for 70B versatile model).

#### 6. Setting Up Tools (Tavily Search) Inside the Function
- `tools = [TavilySearchResults(max_results=2)] if allow_search else []`
  - Uses list comprehension (might seem confusing at first).
  - If `allow_search` is True (user allows web search via checkbox):
    - Add TavilySearchResults tool to the list, limited to top 2 results (`max_results=2`).
    - Fetches only the top 2 online search results.
  - If False: Tools list is empty (no search used).

#### 7. Creating the Agent Inside the Function
- `agent = create_react_agent(model=lm, tools=tools, state_modifier=system_prompt)`
  - Uses the imported `create_react_agent` from LangGraph.
  - Passes:
    - `model=lm`: The LM setup from earlier (based on user-chosen model).
    - `tools=tools`: The search tool (or empty if not allowed).
    - `state_modifier=system_prompt`: Modifies the agent's state based on the system prompt (controls behavior; prompt defined later).

#### 8. Starting Conversation History (State) Inside the Function
- `state = {"messages": [query]}`
  - Initializes the conversation state as a dictionary with a "messages" key.
  - Starts with the current user `query` in the messages list.
  - Purpose: Tracks conversation history (all messages from user so far; maintains context like what was said 5 minutes ago).

#### 9. Running the Agent and Getting Response Inside the Function
- `response = agent.invoke(state)`
  - Runs the agent with the current state (conversation history).
  - Agent processes: Reads messages, thinks, uses search if allowed, generates a reply.
  - Stores the full response (including generated reply) in `response`.

#### 10. Extracting Messages from Response Inside the Function
- `messages = response.get("messages")`
  - Extracts only the "messages" from the response (ignores extra info; focuses on text results).

#### 11. Filtering AI Messages Inside the Function
- `ai_messages = [message.content for message in messages if isinstance(message, AIMessage)]`
  - Uses list comprehension to loop through `messages`.
  - For each `message`, checks if it's an instance of `AIMessage` (using the imported AIMessage class).
    - If yes (AI-generated): Add `message.content` (the text content) to the list.
    - If no (human message/query): Skip it.
  - Purpose: Filters out all AI responses; human queries are in human message instances, AI replies in AIMessage instances.
  - LLMs set instance types: Human for queries, AI for replies.

#### 12. Returning the Latest AI Message Inside the Function
- `return ai_messages[-1]`
  - From the list of AI messages, return only the last one (`[-1]` for list indexing; latest from the end).
  - Purpose: `ai_messages` has all past AI replies (e.g., from 5/4/3 minutes ago), but you want only the latest (final answer to the current query).

#### 13. Usage and Final Notes from Video
- This completes the code for `ai_agent.py` (core AI agent logic).
- The function `get_response_from_ai_agent` will be used in backend and frontend in upcoming videos.
- Correction: At the end, there's a mention of a "system com" (likely typo for "system_prompt") and a spelling mistake in the code; it's fixed to `state_modifier=system_prompt`.
- Proceed to the next video after this.

This function enables multi-agent responses with optional search, conversation tracking, and model selection. For testing/revision, ensure imports work after `pip install -e .`, and note potential tongue slips in transcript (e.g., "modules" for "models", "Brock" for "Groq").

